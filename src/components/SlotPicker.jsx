import React from 'react'
export default function SlotPicker({value,onChange}){const days=[...Array(5)].map((_,i)=>{const d=new Date();d.setDate(d.getDate()+i+1);d.setHours(0,0,0,0);return d});const slots=[];for(const d of days){for(const h of [10,12,15,17]){const s=new Date(d);s.setHours(h,0,0,0);if(s.getTime()>Date.now()+3600000){slots.push(s.toISOString())}}}return(<select value={value||''} onChange={e=>onChange(e.target.value)}><option value='' disabled>Selecciona fecha y hora</option>{slots.map(x=><option key={x} value={x}>{new Date(x).toLocaleString()}</option>)}</select>)}